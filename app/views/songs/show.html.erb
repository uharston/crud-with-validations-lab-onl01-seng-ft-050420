<h1>Show Page</h1>

    <%= @song.title %>
    <%= @song.released %>
    <%= @song.release_year %>
    <%= @song.artist_name %>
    <%= @song.genre %>



<%= link_to "New Song", new_song_path %>
<%= link_to "All Songs", songs_path %>
<%= link_to "Edit", edit_song_path(@song) %>